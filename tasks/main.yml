#
#
#

- name: add repositories and update
  include: apt.yml
  when: generic_manage_apt
  tag: apt

- name: installing unattended upgrades
  include: unattended-upgrades.yml
  when: enable_unattended_upgrade
  tag: apt

- name: add ldap config
  include: add_ldap.yml
  when: use_ldap
  tag: ldap
